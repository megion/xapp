# xapp - test web application for testing Spring, JPA, EJB, SOAP and other technology 

## Build and run

Clone project and run by jetty:

    $ git clone git@github.com:megion/xapp.git
    $ cd xapp
    $ mvn clean install
    $ cd xapp-web
    $ mvn jetty:run